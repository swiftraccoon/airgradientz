name: Conformance

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  conformance:
    name: Cross-implementation conformance
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ncat jq sqlite3 curl bc gcc-14

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - uses: actions/setup-go@v5
        with:
          go-version-file: go/go.mod

      - name: Make conformance scripts executable
        run: chmod +x conformance/*.sh

      - name: Run conformance tests
        run: CC=gcc-14 ./conformance/run.sh nodejs go c
